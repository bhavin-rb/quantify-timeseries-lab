services:
  server:
    build:
      context: ./server
    container_name: timeseries-lab-server
    ports:
      - "5000:5000"
    environment:
      PORT: 5000
      TIINGO_API_KEY: ${TIINGO_API_KEY}
    restart: unless-stopped

  client:
    build:
      context: ./client
    container_name: timeseries-lab-client
    ports:
      - "8080:80"
    environment:
      PORT: 80
      API_PROXY_SCHEME: http
      API_PROXY_HOST: server
      API_PROXY_PORT: "5000"
    depends_on:
      - server
    restart: unless-stopped